<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:paddingBottom="@dimen/activity_vertical_margin"
              android:paddingLeft="@dimen/activity_horizontal_margin"
              android:paddingRight="@dimen/activity_horizontal_margin"
              android:gravity="bottom"
              android:paddingTop="@dimen/activity_vertical_margin">

    <Button
        android:id="@+id/home_map_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/map_button_label"
        android:background="@color/white"
        android:onClick="onClick"/>


    <Button
        android:id="@+id/home_status_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/status_button_label"
        android:layout_below="@id/home_map_button"
        android:background="@color/white"
        android:onClick="onClick"/>

    <Button
        android:id="@+id/home_social_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/social_button_label"
        android:layout_below="@id/home_status_button"
        android:background="@color/white"
        android:onClick="onClick"/>

</RelativeLayout>
